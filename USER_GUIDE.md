# Citadel Release 用户指南

详细的使用说明和技术文档。

## 目录

1. [快速入门](#快速入门)
2. [下载CLI详解](#下载cli详解)
3. [转换CLI详解](#转换cli详解)
4. [数据格式说明](#数据格式说明)
5. [技术原理](#技术原理)
6. [故障排除](#故障排除)

---

## 快速入门

5分钟完成环境配置和首次数据处理。

### 步骤1：安装环境

```bash
# 确保已安装pixi
curl -fsSL https://pixi.sh/install.sh | bash

# 进入项目目录并安装依赖
cd /data/maozan/code/Citadel_release
pixi install
```

### 步骤2：配置mc工具

确保mc工具已配置BOS访问：

```bash
# 检查mc是否可用
/home/maozan/mc --version

# 如果未配置，添加BOS别名
/home/maozan/mc alias set bos <endpoint> <access-key> <secret-key>

# 验证连接
/home/maozan/mc ls bos/
```

### 步骤3：下载数据

```bash
pixi run download \
  --bos-path "srgdata/robot/raw_data/upload_test/online_test_hdf5_v1/fold_laundry/" \
  --local-path "./test_data/download_test/"
```

### 步骤4：转换数据

```bash
pixi run convert \
  --input-dir "./test_data/download_test/" \
  --output-dir "./test_data/convert_test/"
```

完成！转换后的LeRobot v2.1格式数据已保存在输出目录。

---

## 下载CLI详解

### 命令格式

```bash
pixi run download [OPTIONS]
```

### 完整参数列表

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--bos-path` | string | `srgdata/robot/raw_data/.../fold_laundry/` | BOS远程路径 |
| `--local-path` | string | `test_data/download_test/` | 本地保存目录 |
| `--concurrency` | int | `10` | 并发下载数（推荐10-15） |
| `--mc-path` | string | `/home/maozan/mc` | mc可执行文件路径 |

### 使用示例

**基础用法：**
```bash
pixi run download
```

**自定义路径：**
```bash
pixi run download \
  --bos-path "srgdata/robot/raw_data/other_task/" \
  --local-path "/data/project/raw/"
```

**高并发下载：**
```bash
pixi run download --concurrency 15
```

### 输出说明

下载过程中会显示：
- BOS连接状态
- 文件下载进度（逐个显示）
- 完成统计（文件数、总大小、速度）

```
================================================================================
📥 BOS下载工具 - Citadel Release
================================================================================
BOS路径: bos/srgdata/...
本地路径: /home/user/data/
并发数: 10
================================================================================

✓ mc工具已找到: /home/maozan/mc

🔗 检查BOS连接...
✓ BOS连接成功
✓ 本地目录已创建

🚀 开始下载...

[ 1] ✓ episode_0001.h5
[ 2] ✓ episode_0002.h5
...

================================================================================
📊 下载完成统计
================================================================================
下载文件数: 18
总大小: 246.00 MB
总耗时: 16.0秒
平均速度: 15.22 MB/s
================================================================================
```

---

## 转换CLI详解

### 命令格式

```bash
pixi run convert [OPTIONS]
```

### 完整参数列表

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--input-dir` | string | `test_data/download_test/` | 输入HDF5目录 |
| `--output-dir` | string | `test_data/convert_test/` | 输出LeRobot目录 |
| `--robot-type` | string | `airbot_play` | 机器人类型标识 |
| `--fps` | int | `25` | 输出视频帧率 |
| `--task` | string | `Fold the laundry` | 任务描述 |
| `--parallel-jobs` | int | `4` | 并发转换数 |
| `--file-pattern` | string | `episode_*.h5` | 文件匹配模式 |

### 使用示例

**基础用法：**
```bash
pixi run convert
```

**指定机器人类型和任务：**
```bash
pixi run convert \
  --robot-type "custom_robot" \
  --task "Pick and place objects"
```

**高并发转换：**
```bash
pixi run convert --parallel-jobs 8
```

**转换特定文件：**
```bash
pixi run convert --file-pattern "episode_000[1-5].h5"
```

### 输出说明

```
================================================================================
🔄 HDF5批量转换工具 - Citadel Release
================================================================================
输入目录: /home/user/data/raw/
输出目录: /home/user/data/lerobot/
机器人类型: airbot_play
FPS: 25
任务: Fold the laundry
并发数: 4
================================================================================

📦 找到 18 个HDF5文件:
  - episode_0001.h5
  - episode_0002.h5
  ...

🚀 开始批量转换（4个并发任务）...

[ 1/18] ✓ episode_0001.h5           (1.2s)
[ 2/18] ✓ episode_0002.h5           (0.9s)
...

================================================================================
📊 转换完成统计
================================================================================
总文件数: 18
✓ 成功: 18
⏱️  总耗时: 17.0秒 (平均 1.0秒/文件)
================================================================================
✅ 所有文件转换成功！
================================================================================
```

---

## 数据格式说明

### 输入HDF5结构

本工具支持的HDF5文件格式：

```
episode_XXXX.h5
│
├── images/                          # 图像数据
│   ├── cam_env/
│   │   ├── frames_jpeg              # 环境相机帧 (JPEG压缩)
│   │   ├── timestamp_sec            # 秒级时间戳
│   │   └── timestamp_nanosec        # 纳秒级时间戳
│   ├── cam_left/
│   │   ├── frames_jpeg              # 左手相机帧
│   │   ├── timestamp_sec
│   │   └── timestamp_nanosec
│   └── cam_right/
│       ├── frames_jpeg              # 右手相机帧
│       ├── timestamp_sec
│       └── timestamp_nanosec
│
├── joints/                          # 关节数据 (250Hz)
│   ├── left_master/                 # 左臂主控
│   │   ├── joint1_pos ~ joint6_pos  # 6个关节位置
│   │   ├── eef_gripper_joint_pos    # 末端夹爪
│   │   ├── timestamp_sec
│   │   └── timestamp_nanosec
│   ├── right_master/                # 右臂主控
│   │   └── (同上)
│   ├── left_slave/                  # 左臂从控
│   │   └── (同上)
│   └── right_slave/                 # 右臂从控
│       └── (同上)
│
└── metadata                         # 元数据
```

**数据特点：**

| 数据类型 | 采样率 | 说明 |
|----------|--------|------|
| 图像 | ~26 fps | JPEG压缩，三个相机帧数可能不一致 |
| 关节 | 250 Hz | 高频采样，需要下采样对齐到图像帧率 |

### 输出LeRobot v2.1结构

```
episode_XXXX/
│
├── meta/
│   ├── info.json                    # 数据集元信息
│   │   ├── codebase_version: "v2.1"
│   │   ├── robot_type
│   │   ├── fps
│   │   ├── features (state/action维度等)
│   │   └── ...
│   └── tasks.json                   # 任务描述
│
├── data/
│   └── chunk-000/
│       └── episode_000000.parquet   # 状态/动作数据
│           ├── timestamp
│           ├── frame_index
│           ├── task_index
│           ├── state [14维]
│           └── action [14维]
│
└── videos/
    └── chunk-000/
        └── episode_000000/
            ├── cam_env.mp4          # 环境相机视频
            ├── cam_left.mp4         # 左手相机视频
            └── cam_right.mp4        # 右手相机视频
```

**State/Action结构（14维）：**

| 索引 | 内容 | 说明 |
|------|------|------|
| 0-5 | left_joint1_pos ~ left_joint6_pos | 左臂6关节 |
| 6 | left_gripper_pos | 左夹爪 |
| 7-12 | right_joint1_pos ~ right_joint6_pos | 右臂6关节 |
| 13 | right_gripper_pos | 右夹爪 |

- **State**: 从 slave 数据读取（机器人实际状态）
- **Action**: 从 master 数据读取（人类示教动作）

---

## 技术原理

### 多相机时间对齐

三个相机（cam_env, cam_left, cam_right）的帧数可能不一致。转换过程：

1. **确定基准帧数**：取三个相机中最少的帧数
2. **截断多余帧**：其他相机对齐到基准帧数
3. **保持时间同步**：使用各相机自己的时间戳

### 关节数据对齐

关节数据采样率(250Hz)远高于图像帧率(~26fps)。对齐过程：

1. **读取基准时间戳**：使用图像帧的时间戳作为基准
2. **最近邻插值**：对每个图像帧时间戳，找到最近的关节数据
3. **分别处理**：left_slave和right_slave使用各自的时间戳对齐

**对齐精度**：
- 平均对齐误差：1-9ms
- 最大对齐误差：<30ms

### JPEG图像解码

输入HDF5中的图像以JPEG格式压缩存储。转换过程：

```python
# 读取JPEG字节
jpeg_bytes = f["images/cam_env/frames_jpeg"][frame_idx]

# 解码为PIL图像
image = Image.open(io.BytesIO(jpeg_bytes))

# 转换为numpy数组
rgb_array = np.array(image)
```

---

## 故障排除

### 常见错误

#### 1. "mc命令未找到"

```
❌ 错误: mc命令未找到: /home/maozan/mc
```

**解决方案：**
- 确认mc已安装
- 使用 `--mc-path` 指定正确路径
- 或安装mc: `wget https://dl.min.io/client/mc/release/linux-amd64/mc`

#### 2. "BOS连接失败"

```
❌ BOS连接失败: 连接超时
```

**解决方案：**
- 检查网络连接
- 验证mc别名配置: `mc alias list`
- 重新配置: `mc alias set bos <endpoint> <access-key> <secret-key>`

#### 3. "IndexError: index out of bounds"

```
IndexError: index 7492 is out of bounds for axis 0 with size 7492
```

**原因**：左右臂关节数据点数不一致

**解决方案**：已在最新版本修复。确保使用最新的convert.py脚本。

#### 4. "转换超时"

```
转换超时（>5分钟）
```

**解决方案：**
- 检查HDF5文件是否损坏
- 减少并发数: `--parallel-jobs 2`
- 检查系统资源使用

#### 5. "HDF5文件格式不匹配"

```
KeyError: 'images/cam_env/frames_jpeg'
```

**解决方案：**
- 确认HDF5文件结构符合预期格式
- 使用h5dump检查文件结构: `h5dump -H file.h5`

### 调试技巧

#### 查看HDF5文件结构

```bash
# 使用h5dump查看结构
h5dump -H episode_0001.h5

# 使用Python交互式查看
python -c "
import h5py
with h5py.File('episode_0001.h5', 'r') as f:
    def print_structure(name, obj):
        print(name)
    f.visititems(print_structure)
"
```

#### 单独转换单个文件

```bash
python scripts/convert.py \
  --hdf5-path "test_data/download_test/episode_0001.h5" \
  --output-dir "test_data/debug_test/episode_0001/" \
  --robot-type "airbot_play" \
  --fps 25 \
  --task "Debug test"
```

#### 检查转换结果

```bash
# 查看输出目录结构
ls -la test_data/convert_test/episode_0001/

# 检查parquet文件
python -c "
import pandas as pd
df = pd.read_parquet('test_data/convert_test/episode_0001/data/chunk-000/episode_000000.parquet')
print(df.head())
print(f'Columns: {df.columns.tolist()}')
print(f'Shape: {df.shape}')
"

# 检查视频文件
ffprobe test_data/convert_test/episode_0001/videos/chunk-000/episode_000000/cam_env.mp4
```

---

## 性能优化建议

### 下载优化

- **并发数**：网络良好时使用 `--concurrency 15`
- **分批下载**：大量文件时分批处理，避免中断后重新开始

### 转换优化

- **并发数**：根据CPU核心数调整，推荐 `--parallel-jobs $(nproc)`
- **SSD存储**：输入输出目录使用SSD可显著提升速度
- **内存监控**：高并发转换时注意内存使用

### 典型性能

| 环境 | 下载速度 | 转换速度 |
|------|----------|----------|
| 内网+SSD | 15+ MB/s | 1秒/文件 |
| 外网+HDD | 5-10 MB/s | 2-3秒/文件 |

---

**文档版本**: v0.1.0
**最后更新**: 2025-12-28
