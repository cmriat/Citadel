# =============================================================================
# Citadel Release 环境变量配置模板
# =============================================================================
# 复制此文件为 .env 并根据您的环境修改配置值
# cp .env.example .env
# =============================================================================

# =============================================================================
# 服务器配置
# =============================================================================

# API 服务器监听地址 (默认: 0.0.0.0)
# API_HOST=0.0.0.0

# API 服务器端口 (默认: 8000)
# API_PORT=8000

# 前端开发服务器端口 (默认: 5173)
# FRONTEND_PORT=5173

# CORS 允许的源，多个用逗号分隔，* 表示允许所有 (默认: *)
# 生产环境建议设置为具体域名，如: http://localhost:5173,http://example.com
# CORS_ORIGINS=*

# =============================================================================
# BOS 配置
# =============================================================================

# BOS 别名 - mc alias 配置的名称 (默认: bos)
# BOS_ALIAS=bos

# BOS 连接测试路径 (默认: srgdata/)
# BOS_TEST_PATH=srgdata/

# BOS 默认路径前缀 (默认: srgdata/robot/)
# BOS_DEFAULT_PREFIX=srgdata/robot/

# =============================================================================
# MC 工具配置
# =============================================================================

# mc 可执行文件路径，为空时自动检测 (自动检测顺序: MC_PATH > ~/bin/mc > PATH)
# MC_PATH=/usr/local/bin/mc

# =============================================================================
# 数据库配置
# =============================================================================

# SQLite 数据库文件路径 (默认: backend/data/tasks.db)
# DB_PATH=backend/data/tasks.db

# Postgres 配置（用于 QC 结果同步/存在性校验）
# POSTGRES_HOST=172.16.0.24
# POSTGRES_PORT=5432
# POSTGRES_DB=data_manage
# POSTGRES_USER=external_user
# POSTGRES_PASSWORD=external_user

# =============================================================================
# 业务默认值 - 并发和性能
# =============================================================================

# 默认并发数 - 用于下载和上传 (默认: 10)
# DEFAULT_CONCURRENCY=10

# 默认并行任务数 - 用于转换 (默认: 4)
# DEFAULT_PARALLEL_JOBS=4

# =============================================================================
# 业务默认值 - 数据格式
# =============================================================================

# 默认视频帧率 (默认: 25)
# DEFAULT_FPS=25

# 默认机器人类型 (默认: airbot_play)
# DEFAULT_ROBOT_TYPE=airbot_play

# 默认任务描述 (默认: Fold the laundry)
# DEFAULT_TASK_NAME=Fold the laundry

# 默认文件匹配模式 (默认: episode_*.h5)
# DEFAULT_FILE_PATTERN=episode_*.h5

# =============================================================================
# 业务默认值 - 向量维度
# =============================================================================

# 状态向量最大维度 (默认: 14)
# STATE_MAX_DIM=14

# 动作向量最大维度 (默认: 14)
# ACTION_MAX_DIM=14

# =============================================================================
# 超时配置
# =============================================================================

# mc 工具检查超时（秒）(默认: 10)
# TIMEOUT_MC_CHECK=10

# BOS 扫描超时（秒）(默认: 30)
# TIMEOUT_BOS_SCAN=30

# 单文件转换超时（秒）(默认: 300，即 5 分钟)
# TIMEOUT_CONVERT=300

# 上传任务超时（秒）(默认: 3600，即 1 小时)
# TIMEOUT_UPLOAD=3600

# API 请求默认超时（毫秒）(默认: 30000，即 30 秒)
# TIMEOUT_API_DEFAULT=30000

# Pipeline 操作超时（毫秒）(默认: 120000，即 2 分钟)
# TIMEOUT_PIPELINE=120000

# =============================================================================
# 其他配置
# =============================================================================

# 自动清理超过指定天数的已完成任务 (默认: 30)
# TASK_CLEANUP_DAYS=30

# JPEG 图片压缩质量 0-100 (默认: 70)
# JPEG_QUALITY=70

# 缩略图缓存目录（用于加速 QC/scan-episodes，默认: <DB_PATH 同级>/cache/thumbnails）
# THUMBNAIL_CACHE_DIR=backend/data/cache/thumbnails

# 缩略图内存缓存最大条目数（LRU，默认: 512）
# THUMBNAIL_CACHE_MAX_ITEMS=512

# 调试模式 (默认: false)
# DEBUG=false
